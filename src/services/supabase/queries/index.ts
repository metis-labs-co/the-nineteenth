/**
 * Typed Supabase Query Helpers
 *
 * Import from here for clean access to all typed query functions:
 *
 * ```ts
 * import {
 *   fetchTeamsWithMembers,
 *   fetchRoundWithCourse,
 *   fetchCompetitionPlayers,
 * } from '@/services/supabase/queries';
 * ```
 */

export * from './teams';
